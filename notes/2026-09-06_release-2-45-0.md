# 2026-09-06 — Release 2.45.0 (v2026.9.6-0): hardening pass

Cut the `2.45.0` release bundling the seven fixes that landed from the xAI
Grok-Build comparison pass (`research/2026-08-23_grok-build-comparison.md`),
merged as #486–#492.

## What shipped

| # | Area | Fix |
|---|---|---|
| #486 | provider | HTTP 429/408 retryable + `Retry-After` (clamped) |
| #487 | policy | guardrails peel command wrappers (`argv::peel`, fail-closed) |
| #492 | policy | allow-pattern full-coverage gate; unparseable args fail closed |
| #489 | chat | recover operator text typed during a permission prompt |
| #490 | tui | RAII terminal-restore guard on panic (3 surfaces) |
| #491 | deep-memory | insert dedup + embedding-model stamp (model-switch safe) |
| #488 | tools | 64 KiB tool-output cap + `read_file` `offset`/`limit` |

Several are security-relevant (guardrail wrapper bypass, allow-substring
under-block, 429-abort). No breaking changes → **minor** bump (Cargo 2.44.2 →
2.45.0; CalVer `v2026.9.6-0`).

## Release steps done

- `scripts/bump-version.sh minor --software` → 2.45.0.
- `whats_new.rs` HEADLINE + HIGHLIGHTS refreshed (all six bullets cite 2.45.0 —
  satisfies the `highlights_cite_current_version` release gate).
- `CHANGELOG.md` — new `v2026.9.6-0 — 2.45.0` section.
- `VERSION.md` + `README.md` "Latest release" pointers bumped (the release gate
  pins these to the topmost CHANGELOG entry). `Formula/bwoc.rb` intentionally
  left at the prior tag — it trails by one release and is bumped by `release.yml`
  after the artifacts (and their checksums) exist.
- Note = this file.

## Deferred / next

- Tag `v2026.9.6-0` pushed **after** this PR merges → `release.yml` builds the
  5-platform archives + bumps the Homebrew formula.

## Related

- PRs #486–#492; source `research/2026-08-23_grok-build-comparison.md`.
