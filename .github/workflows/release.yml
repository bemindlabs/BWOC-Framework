name: Release

# Release identity uses CalVer: v<YYYY>.<M>.<D>-<patch>
# Examples: v2026.5.22-0, v2026.5.22-1, v2026.12.31-9
#
# Cargo.toml continues to use SemVer for development (auto-bumped by
# .claude/hooks/auto-version.sh). The two coexist intentionally — see
# VERSION.md "Dual Versioning" for the why. Cargo SemVer = internal dev
# checkpoint; Git tag CalVer = public release identity.
#
# Maintainers push the tag manually:
#
#   git tag v2026.5.22-0
#   git push origin v2026.5.22-0
#
# Code-signing is NOT done yet — binaries are unsigned, distributed with
# SHA-256 checksums. Adding signing requires per-platform cert setup
# (Apple Developer for macOS, Windows Authenticode); deferred until the
# user authorizes the certs and storage location.
on:
  push:
    tags:
      # CalVer-shaped tags only — refuses ad-hoc semver tags like v0.1.0.
      # If a pre-CalVer fallback is ever needed, widen this glob carefully.
      - 'v[0-9][0-9][0-9][0-9].*'

permissions:
  contents: write # create / upload to the GitHub Release; push the formula branch

# Forward-compat for the Node 24 cutover (2026-06-02). `actions/checkout@v6`
# runs on Node 24 natively; `softprops/action-gh-release@v2` still defaults to
# Node 20 (unsupported 2026-09-16), so the env below forces it onto Node 24,
# keeping the deprecation banner gone through the cutover window.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Force HTTP/1.1 + retry for crates.io fetches — avoids the intermittent
  # "Error in the HTTP2 framing layer" curl transient (see ci.yml).
  CARGO_HTTP_MULTIPLEXING: false
  CARGO_NET_RETRY: 10

jobs:
  # Create the release shell once before any matrix builds — eliminates
  # the race condition where parallel matrix jobs would each call
  # softprops/action-gh-release@v2 and one would lose with
  # "Validation Failed: already_exists". Now matrix jobs find the
  # release pre-existing and just append assets.
  create-release:
    name: create release shell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Create empty release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF##refs/tags/}"
          # Idempotent: if a Release object for this tag already exists (a tag
          # re-push, a re-run, or a race that created it first), `gh release
          # create` 422s ("already_exists") and fails the whole job — which
          # `needs:`-chains build + bump-formula into `skipped`, shipping a
          # release with zero binaries (issue #371). Reuse the existing shell
          # instead; the matrix build jobs append their assets to it.
          # --notes-start-tag=$prev would be ideal but `gh release create`
          # auto-generates from history when --generate-notes is set;
          # safer for the first release where no prior tag exists.
          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $tag already exists — reusing it (matrix jobs append assets)."
          else
            gh release create "$tag" \
              --title "$tag" \
              --generate-notes \
              --repo "$GITHUB_REPOSITORY"
          fi

  build:
    name: build ${{ matrix.target }} (${{ matrix.os }})
    needs: create-release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive_ext: tar.gz
          # Native ARM Linux build. GitHub offers `ubuntu-24.04-arm`
          # free for public repos — no cross-compile, no `cross` tool.
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            archive_ext: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            archive_ext: tar.gz
          - os: macos-latest
            target: x86_64-apple-darwin
            archive_ext: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive_ext: zip

    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: cargo build (release)
        env:
          BWOC_RELEASE_CALVER: ${{ github.ref_name }}
        run: cargo build --release --workspace --target ${{ matrix.target }}

      - name: Package (tar.gz)
        if: matrix.archive_ext == 'tar.gz'
        shell: bash
        run: |
          mkdir -p dist
          tag="${GITHUB_REF##refs/tags/}"
          name="bwoc-${tag}-${{ matrix.target }}"
          mkdir "$name"
          # All THREE binaries. `bwoc-harness` runs the agentic loop and `bwoc`
          # spawns it by name (next to itself → CARGO_BIN_EXE → PATH), so an
          # archive without it yields an install whose `chat --tui`, `eval`,
          # `--headless`, `--lead` and `--task` paths all fail at spawn (#460).
          cp target/${{ matrix.target }}/release/bwoc         "$name/"
          cp target/${{ matrix.target }}/release/bwoc-agent   "$name/"
          cp target/${{ matrix.target }}/release/bwoc-harness "$name/"
          cp README.md LICENSE CHANGELOG.md                   "$name/" 2>/dev/null || true
          # Assert before publishing: #460 shipped silently for many releases
          # because packaging was never checked. Fail loudly instead.
          for b in bwoc bwoc-agent bwoc-harness; do
            test -f "$name/$b" || { echo "::error::$b missing from $name"; exit 1; }
          done
          tar -czf "dist/${name}.tar.gz" "$name"
          shasum -a 256 "dist/${name}.tar.gz" > "dist/${name}.tar.gz.sha256"

      - name: Package (zip)
        if: matrix.archive_ext == 'zip'
        shell: pwsh
        run: |
          $tag = "${env:GITHUB_REF}" -replace '^refs/tags/', ''
          $name = "bwoc-$tag-${{ matrix.target }}"
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          New-Item -ItemType Directory -Force -Path $name | Out-Null
          # All THREE binaries — see the tar.gz step for why the harness must ship (#460).
          Copy-Item "target/${{ matrix.target }}/release/bwoc.exe"         -Destination "$name/"
          Copy-Item "target/${{ matrix.target }}/release/bwoc-agent.exe"   -Destination "$name/"
          Copy-Item "target/${{ matrix.target }}/release/bwoc-harness.exe" -Destination "$name/"
          Copy-Item README.md, LICENSE, CHANGELOG.md -Destination "$name/" -ErrorAction SilentlyContinue
          foreach ($b in @('bwoc.exe','bwoc-agent.exe','bwoc-harness.exe')) {
            if (-not (Test-Path "$name/$b")) { Write-Error "::error::$b missing from $name"; exit 1 }
          }
          Compress-Archive -Path "$name" -DestinationPath "dist/$name.zip"
          $h = (Get-FileHash -Algorithm SHA256 "dist/$name.zip").Hash.ToLower()
          "$h  $name.zip" | Out-File -Encoding ascii "dist/$name.zip.sha256"

      - name: Upload assets to existing release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          tag="${GITHUB_REF##refs/tags/}"
          # Upload every artifact in dist/ to the release created by
          # the create-release job. `gh release upload --clobber` is
          # idempotent — replays of a rerun-after-failure scenario won't
          # error on duplicate asset names.
          gh release upload "$tag" dist/* --clobber --repo "$GITHUB_REPOSITORY"

  # Keep the Homebrew tap honest (issue #52). `needs: build` => all .sha256
  # sidecars exist before we rewrite Formula/bwoc.rb from them.
  #
  # The tap serves the default branch's HEAD and `main` is protected, so the
  # bump must land via an auto-merging PR — never a direct push. The default
  # GITHUB_TOKEN can't open that PR (the org blocks Actions from opening PRs,
  # and a GITHUB_TOKEN-opened PR wouldn't fire CI anyway), so this job pushes
  # the formula branch and prints the one finish command in the job summary.
  # It never fails the release.
  bump-formula:
    name: bump Homebrew formula
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # Commit onto the default branch (the tap serves its HEAD), not
          # the detached tag ref the push event would otherwise check out.
          ref: ${{ github.event.repository.default_branch }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Rewrite Formula/bwoc.rb from the released sidecars
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF_NAME}"
          mkdir -p sidecars
          # Only the 4 unix tar.gz sidecars the formula consumes; the
          # Windows .zip.sha256 doesn't match this glob and is skipped.
          gh release download "$tag" \
            --pattern '*.tar.gz.sha256' \
            --dir sidecars \
            --repo "$GITHUB_REPOSITORY"
          ./scripts/bump-formula.sh "$tag" sidecars

      - name: Push a formula-bump branch if the formula changed
        # `main` is protected, so the bump must land via an auto-merged PR,
        # never a direct push. The default GITHUB_TOKEN can't open that PR (org
        # setting), so this step pushes the branch and prints the one finish
        # command in the job summary, then exits 0 so the release run stays green.
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          if git diff --quiet -- Formula/bwoc.rb; then
            echo "Formula already current for ${GITHUB_REF_NAME} — nothing to do."
            exit 0
          fi
          branch="chore/formula-${GITHUB_REF_NAME}"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch -C "$branch"
          git add Formula/bwoc.rb
          git commit \
            -m "chore(formula): bump Homebrew formula to ${GITHUB_REF_NAME}" \
            -m "Auto-generated by release.yml on tag publish; version + 4 url fragments + 4 sha256 synced to the released sidecars. Lands via auto-merge because main is a protected branch."
          # Unconditional `--force`: a rerun of the same tag reuses the branch
          # name, but actions/checkout fetches only the default branch, so there
          # is no `origin/$branch` remote-tracking ref for `--force-with-lease`
          # to verify against (it would reject the rerun). The branch is
          # bot-owned and its content is fully derived from the released
          # sidecars, so overwriting it on a rerun is correct and safe.
          git push --force origin "$branch"
          echo "::notice::Pushed formula branch '$branch'. GITHUB_TOKEN can't open the PR under the org setting — finish with the command in the job summary."
          {
            echo "### Homebrew formula bump — one manual step"
            echo "Branch \`$branch\` was pushed with the \`${GITHUB_REF_NAME}\` formula. Open + auto-merge it:"
            echo ""
            echo '```sh'
            echo "gh pr create --base $DEFAULT_BRANCH --head $branch --fill"
            echo "gh pr merge $branch --squash --auto --delete-branch"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
