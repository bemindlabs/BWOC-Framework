# Template manifest — copied + substituted by `bwoc skill init <name>`.
# Placeholders use the `{{camelCase}}` convention shared with
# `modules/agent-template/` and `modules/plugin-template/`. See SPEC.md
# in this directory for the substitution table.

[skill]
name        = "{{skillName}}"
version     = "{{skillVersion}}"
description = "{{skillDescription}}"
maturity    = "L1"

[contract]
requires    = []
exposes     = ["{{skillOperation}}"]

# Optional: add a [gates] table with `verify = "<shell command>"` when the
# skill has something concrete to check. The gate runs inside
# `bwoc skill verify --run-gates`, so it must not call `bwoc skill verify`
# itself — `bwoc check` rejects that.
